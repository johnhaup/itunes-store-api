/**
 * Encode a value into a URI component but escape spaces as `+`.
 *
 * @param value - The value to encode.
 */
export declare function encodeURIFormComponent(value: boolean | number | string): string;
